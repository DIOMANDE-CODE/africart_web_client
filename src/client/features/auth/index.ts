/**
 * Auth feature public API.
 *
 * Usage:
 *   import { AuthProvider, useAuth } from '../../features/auth';
 */
export { AuthContext, AuthProvider, useAuth } from '../../context/AuthContext';
export type { } from '../../types';   // re-export User via types/index.ts
